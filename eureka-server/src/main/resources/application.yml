server:
  port: ${PORT:7311}

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: localhost
  server:
    enableSelfPreservation: false
  client:
    registerWithEureka: false
    fetchRegistry: false